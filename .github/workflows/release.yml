name: Build and Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build Windows
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup multi-layer cache
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.STORE_PATH }}
            ~/.cache/electron
            ~/.cache/electron-builder
            ${{ github.workspace }}/.cache/electron-builder
          key: ${{ runner.os }}-deps-${{ hashFiles('**/pnpm-lock.yaml') }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-deps-${{ hashFiles('**/pnpm-lock.yaml') }}-
            ${{ runner.os }}-deps-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --prefer-offline

      - name: Cache Next.js build
        uses: actions/cache@v4
        with:
          path: |
            .next/cache
            out
          key: ${{ runner.os }}-nextjs-${{ hashFiles('**/pnpm-lock.yaml') }}-${{ hashFiles('app/**', 'components/**', 'lib/**', 'next.config.js') }}
          restore-keys: |
            ${{ runner.os }}-nextjs-${{ hashFiles('**/pnpm-lock.yaml') }}-
            ${{ runner.os }}-nextjs-

      - name: Cache TypeScript build
        uses: actions/cache@v4
        with:
          path: |
            dist
            **/*.tsbuildinfo
          key: ${{ runner.os }}-typescript-${{ hashFiles('**/pnpm-lock.yaml') }}-${{ hashFiles('**/*.ts', '**/*.tsx', '**/tsconfig.json') }}
          restore-keys: |
            ${{ runner.os }}-typescript-${{ hashFiles('**/pnpm-lock.yaml') }}-
            ${{ runner.os }}-typescript-

      - name: Generate application icons
        run: pnpm build:icon

      - name: Build for Windows
        run: pnpm build:win
        env:
          ELECTRON_BUILDER_CACHE: ${{ github.workspace }}/.cache/electron-builder
          ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES: true
          CI: true

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v5
        with:
          name: windows-build
          path: |
            release/*.exe
            release/*.exe.blockmap
            release/latest.yml

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Download all artifacts
        uses: actions/download-artifact@v6
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -R artifacts

      - name: Generate checksums
        run: |
          cd artifacts/windows-build
          sha256sum *.exe > SHA256SUMS.txt
          sha256sum *.yml >> SHA256SUMS.txt
          cat SHA256SUMS.txt

      - name: Import GPG key
        if: secrets.GPG_PRIVATE_KEY != ''
        run: |
          echo "${{ secrets.GPG_PRIVATE_KEY }}" | gpg --batch --import
          gpg --list-keys

      - name: Sign release files with GPG
        if: secrets.GPG_PRIVATE_KEY != ''
        run: |
          cd artifacts/windows-build
          # Sign all .exe and .yml files
          for file in *.exe *.yml SHA256SUMS.txt; do
            if [ -f "$file" ]; then
              gpg --batch --yes --detach-sign --armor "$file"
              echo "Signed: $file"
            fi
          done
          ls -la

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/windows-build/*.exe
            artifacts/windows-build/*.exe.blockmap
            artifacts/windows-build/*.yml
            artifacts/windows-build/*.asc
            artifacts/windows-build/SHA256SUMS.txt
          draft: false
          prerelease: false
          generate_release_notes: true
          body: |
            ## 🎉 새 버전이 출시되었습니다!

            ### 📥 설치 방법

            **Windows 사용자:**
            1. `SEPilot-Setup-*.exe` 파일 다운로드
            2. 실행 시 Windows SmartScreen 경고가 표시될 수 있습니다
            3. "추가 정보" → "실행" 클릭하여 설치 진행

            ### 🔒 보안 검증

            **SHA256 체크섬 확인:**
            ```bash
            sha256sum -c SHA256SUMS.txt
            ```

            **GPG 서명 검증 (선택사항):**
            ```bash
            # GPG 공개키 가져오기
            gpg --keyserver keys.openpgp.org --recv-keys YOUR_KEY_ID

            # 서명 검증
            gpg --verify SEPilot-Setup-*.exe.asc SEPilot-Setup-*.exe
            ```

            ### ⚙️ 자동 업데이트
            앱 내에서 자동 업데이트가 지원됩니다.

            ---
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
