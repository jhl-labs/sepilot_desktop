# Release Notes - v0.6.13

**출시일**: 2024-12-28

## 🔧 중요 버그 수정: 프록시/SSL 네트워크 정책 통합

### 문제 상황

프록시 환경에서 LLM 연결, 모델 등록, 이미지 생성이 실패하는 치명적인 문제가 있었습니다.

- Settings에서 설정한 프록시/SSL 설정이 일부 HTTP 통신에만 적용됨
- 브라우저 환경에서 NetworkConfig를 로드하지 못하는 버그
- WebSocket 통신에서 프록시 설정 무시

### 해결 내용

#### 1. 전체 HTTP 통신 통합 (`8e14d84`)

**통합 완료된 모듈 (13개 파일):**

**Main Process**

- `electron/ipc/handlers/file.ts` - URL 콘텐츠 fetch
- `electron/ipc/handlers/github.ts` - GitHub OAuth 토큰 교환
- `lib/langgraph/nodes/tools.ts` - ComfyUI 이미지 생성

**Library**

- `lib/llm/web-client.ts` - LLM 스트리밍
- `lib/vectordb/embeddings/openai.ts` - 임베딩 생성

**Frontend Components**

- `components/settings/settingsUtils.ts` - 설정 유틸리티
- `components/settings/GeneralSettingsTab.tsx` - GitHub 릴리즈 확인
- `components/settings/ImageGenSettingsTab.tsx` - ComfyUI 연결 테스트
- `components/settings/ComfyUISettingsTab.tsx` - ComfyUI 연결 테스트
- `components/rag/VectorDBSettings.tsx` - 임베딩 모델 목록 조회

**이미 통합되어 있던 모듈:**

- LLM Providers (OpenAI, Ollama)
- GitHub Sync
- ComfyUI Client
- MCP SSE Transport
- NanoBanana
- Documents Fetchers

**결과:** 모든 HTTP 통신이 `lib/http/httpFetch`를 사용하여 NetworkConfig 자동 적용

#### 2. WebSocket 통신 통합 (`328356f`)

**수정된 파일:**

- `lib/langgraph/nodes/tools.ts` - ComfyUI WebSocket 모니터링에 `createWebSocket` 사용
- `lib/comfyui/client.ts` - 브라우저 WebSocket 제한사항 주석 추가

**주의사항:**

- Electron Main Process: `createWebSocket`으로 NetworkConfig 완전 제어
- 브라우저 환경: WebSocket이 시스템 프록시 설정 자동 적용 (브라우저 제한)

#### 3. NetworkConfig 로드 버그 수정 (`ea4ecde`) ⚡ 중요

**발견한 치명적 버그:**

```typescript
// 저장 (SettingsDialog)
localStorage.setItem('sepilot_network_config', ...)

// 로드 (lib/http/config.ts) - 버그!
localStorage.getItem('sepilot_app_config')  // 잘못된 키!
```

**결과:** 브라우저/Renderer 환경에서 NetworkConfig를 전혀 읽지 못함

**수정 사항:**

`lib/http/config.ts`:

- `loadFromLocalStorage()`: 올바른 키 사용 + fallback 추가
  ```typescript
  // 우선순위 1: sepilot_network_config
  // Fallback: sepilot_app_config의 network 필드
  ```
- `loadViaIPC()`: `result.data.network` 경로 추가, localStorage fallback 추가

## 🎯 사용자 영향

### 해결된 문제

✅ **프록시 환경에서 LLM 통신 실패** → 모든 LLM API 호출에 프록시 적용
✅ **모델 등록 실패** → 모델 목록 조회 API에 프록시 적용
✅ **이미지 생성 실패** → ComfyUI HTTP/WebSocket 통신에 프록시 적용
✅ **브라우저에서 설정 무시** → localStorage 키 버그 수정으로 정상 로드
✅ **SSL 인증서 오류** → 모든 통신에 SSL 검증 설정 적용

### 적용 범위

**모든 환경에서 NetworkConfig 정상 작동:**

- ✅ Electron Main Process: Database
- ✅ Electron Renderer: IPC + localStorage fallback
- ✅ 브라우저: localStorage (수정됨)
- ✅ Node.js: 환경 변수

## 📊 통신 통합 현황

```
✅ HTTP: lib/http/httpFetch
✅ WebSocket: lib/http/createWebSocket
✅ SSE: lib/http/createEventSource
```

**직접 fetch 사용 (정상):**

- `lib/http/fetch.ts` - httpFetch 구현 내부
- `electron/tests/suites/network.ts` - 네트워크 테스트 (2개)

## 🔧 기술적 개선

- 중앙 집중식 HTTP 통신 관리 (`lib/http/`)
- 환경별 NetworkConfig 로드 전략 개선
- 프록시/SSL 설정 일관성 보장
- 에러 분류 및 처리 개선 (HttpError)

## 📦 커밋 내역

- `8e14d84`: feat: 전체 HTTP 통신을 lib/http 모듈로 통합하여 NetworkConfig 일관성 확보
- `328356f`: fix: WebSocket 통신에도 NetworkConfig(프록시/SSL) 적용
- `ea4ecde`: fix: 브라우저 환경에서 NetworkConfig 로드 실패 버그 수정

## 🚀 업그레이드 참고사항

기존 사용자의 설정은 자동으로 마이그레이션됩니다.

- 프록시/SSL 설정을 다시 확인하고 저장하면 즉시 적용됩니다.
- 브라우저 환경 사용자는 설정을 다시 저장하여 localStorage 키 버그를 해결해야 합니다.

---

**전체 변경사항**: [v0.6.12...v0.6.13](https://github.com/jhl-labs/sepilot_desktop/compare/v0.6.12...v0.6.13)
