# SEPilot Desktop v0.7.5 릴리스 노트

**릴리즈 날짜**: 2026-01-07

---

## ✨ 주요 개선사항

### 🚀 Editor Agent Cursor/Cline 수준 고도화

Editor Agent를 Cursor, Cline, Roo Code 수준의 고급 AI 코딩 어시스턴트로 대폭 개선했습니다.

**추가된 핵심 기능:**

#### 1. MCP (Model Context Protocol) Tools 통합

외부 도구와의 통합을 통해 Editor Agent의 능력을 대폭 확장했습니다.

- **MCP Tools 지원**: GitHub, Filesystem, Brave Search 등 다양한 외부 도구 사용 가능
- **동적 도구 검색**: 모든 MCP 서버에서 사용 가능한 도구를 자동으로 검색하여 실행
- **에러 처리 강화**: MCP 도구 실행 실패 시 명확한 에러 메시지 제공

```typescript
// Editor Agent에서 MCP Tools 사용 예시
// GitHub repository 검색, 웹 검색, 파일 시스템 작업 등이 가능합니다
const graphConfig = {
  thinkingMode: 'editor-agent' as const,
  enableRAG: true, // RAG 활성화
  enableTools: true, // Built-in Editor Tools 활성화
  enableMCPTools: true, // MCP Tools 활성화 ⭐ NEW
};
```

#### 2. File Tracking & Rollback 시스템

파일 변경사항을 자동으로 추적하여 안전한 코드 수정을 보장합니다.

- **변경 전/후 추적**: 파일 수정 전후 상태를 자동으로 기록
- **롤백 포인트 생성**: 각 iteration마다 롤백 포인트 생성으로 언제든 이전 상태로 복구 가능
- **변경 이력 관리**: 어떤 파일이 언제 어떻게 변경되었는지 추적

**구현 클래스:**

- `FileTracker`: 파일 변경 추적 및 롤백 관리
- `trackBeforeModify()`: 파일 수정 전 스냅샷 저장
- `trackAfterModify()`: 파일 수정 후 상태 기록
- `createRollbackPoint()`: 복구 포인트 생성

#### 3. Error Recovery & Retry 시스템

도구 실행 실패 시 자동으로 재시도하여 안정성을 높였습니다.

- **자동 재시도**: 일시적 오류 발생 시 최대 2회 자동 재시도
- **타임아웃 관리**: 각 도구 실행에 적절한 타임아웃 설정 (Built-in: 30초, MCP: 60초)
- **지수 백오프**: 재시도 간 대기 시간을 점진적으로 증가시켜 서버 부하 방지

```typescript
// Error Recovery 적용 예시
const result = await ErrorRecovery.withTimeoutAndRetry(
  () => executeBuiltinTool(toolCall.name, toolCall.arguments),
  30000, // 30 second timeout
  { maxRetries: 2, initialDelayMs: 1000 },
  `tool '${toolCall.name}'`
);
```

#### 4. Advanced State Management

CodingAgentState를 기반으로 확장하여 고급 기능을 지원합니다.

- **CodingAgentState 확장**: Editor Agent가 Coding Agent의 모든 기능 활용 가능
- **Planning 지원**: 복잡한 작업을 단계별로 계획하고 실행 (향후 완전 구현 예정)
- **Verification 지원**: 코드 변경 후 자동 검증 (향후 완전 구현 예정)
- **Context Management**: 100k 토큰 관리 시스템 통합 준비

#### 5. RAG (Retrieval-Augmented Generation) 활성화

프로젝트 문서를 기반으로 더 정확한 답변을 제공합니다.

- **문서 검색**: 벡터 DB에서 관련 문서 자동 검색
- **컨텍스트 강화**: 검색된 문서를 프롬프트에 포함하여 정확도 향상
- **에러 핸들링**: RAG 실패 시에도 자체 지식으로 계속 진행

---

## 💡 UX 개선

### Working Directory 경고 시스템

Coding 모드에서 Working Directory가 설정되지 않았을 때 사용자에게 명확한 경고를 제공합니다.

**추가된 기능:**

#### 1. Working Directory 미설정 경고

Coding 모드에서 파일 작업을 시도하기 전에 사용자에게 Working Directory 설정 필요성을 알려줍니다.

- **시각적 경고**: Coding 모드에서 Working Directory가 설정되지 않으면 오렌지색 경고 배너 표시
- **즉시 설정 가능**: 경고 배너에서 바로 "Set Working Directory" 버튼 클릭으로 설정 가능
- **명확한 메시지**: "Working directory not set. File operations will fail." 메시지로 문제 상황 명확히 전달

**적용 파일:**

- `components/chat/WorkingDirectoryIndicator.tsx`: 경고 UI 추가
- `lib/langgraph/prompts/coding-agent-system.ts`: Agent에게 Working Directory 미설정 시 가이드 제공

```typescript
// Working Directory 미설정 시 경고 표시
if (thinkingMode === 'coding' && !workingDirectory) {
  return (
    <div className="border-t border-orange-500/50 bg-orange-500/10">
      <div className="flex items-center gap-2">
        <AlertCircle className="h-4 w-4 text-orange-600" />
        <span className="font-medium text-orange-700">
          Warning: Working directory not set. File operations will fail.
        </span>
        <Button onClick={handleSelectDirectory}>
          Set Working Directory
        </Button>
      </div>
    </div>
  );
}
```

#### 2. Agent 프롬프트 개선

Coding Agent가 Working Directory가 설정되지 않았을 때 적절히 대응할 수 있도록 프롬프트를 개선했습니다.

**Agent에게 제공되는 가이드:**

- 파일 작업 시도 전에 사용자에게 Working Directory 설정 요청
- 명확한 안내 메시지 제공
- 불필요한 작업 시도 방지

```typescript
// Agent 프롬프트에 추가된 경고
⚠️ CRITICAL: No Working Directory Set

**What You Should Do**:
1. Immediately inform the user that working directory is not set
2. Ask them to select a working directory before proceeding
3. DO NOT attempt any file or command operations
4. Explain that they need to click "Set Working Directory" in Coding mode
```

---

## 🛠️ 터미널 도구 추가

Editor Extension에 터미널 작업을 지원하는 새로운 도구들을 추가했습니다.

**추가된 도구:**

### 1. get_history - 명령어 히스토리 조회

Store에서 최근 명령어 히스토리를 조회합니다.

```typescript
// get_history Tool 사용 예시
const result = await executeGetHistory({
  limit: 10, // 최대 10개 조회
  cwd: '/path/to/project', // 특정 디렉토리 필터링 (선택)
  includeOutput: false, // 출력 포함 여부 (선택)
});
```

**기능:**

- 최근 명령어 히스토리 조회 (기본 10개)
- Working directory 기준 필터링
- 명령어 출력 포함/제외 옵션

**파일:** `extensions/terminal/tools/get_history.ts`

### 2. run_command - 쉘 명령어 실행

PTY Manager를 통해 shell 명령어를 실행하고 결과를 반환합니다.

```typescript
// run_command Tool 사용 예시
const result = await executeRunCommand({
  command: 'npm test', // 실행할 명령어
  cwd: '/path/to/project', // Working directory (선택)
  timeout: 30000, // 타임아웃 (기본 30초)
});
```

**기능:**

- 쉘 명령어 실행
- 표준 출력/에러 반환
- 타임아웃 설정 (기본 30초)
- Exit code 반환

**파일:** `extensions/terminal/tools/run_command.ts`

### 3. search_commands - RAG 기반 명령어 검색

유사한 명령어를 검색합니다 (현재는 키워드 기반, Phase 5에서 RAG 완전 구현 예정).

```typescript
// search_commands Tool 사용 예시
const result = await executeSearchCommands({
  query: 'install dependencies', // 검색 쿼리
  topK: 5, // 반환할 결과 수 (기본 5개)
});
```

**기능:**

- 자연어 쿼리로 명령어 검색
- 키워드 기반 매칭 (현재)
- Vector DB 기반 시맨틱 검색 (Phase 5 예정)

**파일:** `extensions/terminal/tools/search_commands.ts`

### 4. explain_error - LLM 기반 에러 분석

LLM을 사용하여 에러 메시지를 분석하고 해결책을 제안합니다.

```typescript
// explain_error Tool 사용 예시
const result = await executeExplainError({
  errorMessage: 'npm ERR! code ENOENT',
  command: 'npm install',
  exitCode: 1,
  cwd: '/path/to/project',
});

// Result
{
  success: true,
  analysis: {
    cause: 'package.json file not found',
    solutions: [
      'Run npm init to create package.json',
      'Check if you are in the correct directory',
      'Verify the file exists'
    ],
    summary: 'Missing package.json file'
  }
}
```

**기능:**

- 에러 메시지 자동 분석
- 원인 파악 및 해결책 제안
- LLM 기반 지능형 분석 (temperature: 0.3)

**파일:** `extensions/terminal/tools/explain_error.ts`

---

## 🔐 보안 및 안정성 개선

### components/ 디렉토리 대규모 리팩토링

프로덕션 환경을 위한 보안, 안정성, UX 개선 작업을 수행했습니다.

#### 1. XSS 취약점 수정

**문제**: `dangerouslySetInnerHTML` 사용 시 XSS 공격 위험

**해결책**:

- DOMPurify 패키지 추가하여 모든 HTML 콘텐츠 sanitize
- 적용 파일:
  - `components/markdown/MermaidDiagram.tsx`: SVG 렌더링 시 sanitize 적용
  - `components/settings/QuickInputSettingsTab.tsx`: i18n 번역 텍스트 sanitize

```typescript
// Before
<div dangerouslySetInnerHTML={{ __html: svgCode }} />

// After
const sanitizedSvg = DOMPurify.sanitize(svgCode, {
  USE_PROFILES: { svg: true, svgFilters: true },
});
<div dangerouslySetInnerHTML={{ __html: sanitizedSvg }} />
```

#### 2. Error Boundary 추가

**문제**: 컴포넌트 렌더링 에러 시 앱 전체 크래시

**해결책**:

- `ErrorBoundary` 컴포넌트 생성
- 주요 컴포넌트에 Error Boundary 적용:
  - `MarkdownRenderer`: Mermaid/Plotly 차트 렌더링 실패 시 폴백
  - `ChatContainer`, `BrowserChatContainer`, `EditorChatContainer`: UnifiedChatArea 에러 격리

```typescript
<ErrorBoundary fallback={<CodeBlock language="mermaid" code={codeText} />}>
  <MermaidDiagram chart={codeText} />
</ErrorBoundary>
```

#### 3. UX 개선 - Toast 알림 시스템

**문제**: `window.alert` 사용으로 인한 UX 저하

**해결책**:

- Sonner toast 라이브러리 추가
- 모든 `window.alert`를 toast로 전환 (10개 파일, 총 12회)
- 적용 파일:
  - `components/chat/unified/UnifiedChatArea.tsx`
  - `components/chat/ChatArea.tsx`
  - `components/settings/QuickInputSettingsTab.tsx`
  - `components/settings/ConnectionManager.tsx`
  - `components/persona/PersonaDialog.tsx`

```typescript
// Before
window.alert('메시지가 Document로 저장되었습니다.');

// After
toast.success('메시지가 Document로 저장되었습니다.');
```

#### 4. 로깅 시스템 개선

**문제**: 프로덕션 코드에 `console.log` 남아있음 (217회)

**해결책**:

- 주요 파일의 `console.log/error`를 `logger` 유틸리티로 전환
- 개발 환경에서만 로그 출력하도록 설정
- 적용 파일:
  - `components/chat/ChatContainer.tsx`
  - `components/chat/unified/UnifiedChatArea.tsx`
  - `components/markdown/MermaidDiagram.tsx`

```typescript
// Before
console.error('[ChatContainer] Failed to abort stream:', error);

// After
logger.error('[ChatContainer] Failed to abort stream:', error);
```

---

## 🔧 기술적 개선

### 아키텍처 변경사항

**1. EditorAgentState 확장** (`extensions/editor/agents/editor-agent-advanced.ts`)

```typescript
// Before
export interface EditorAgentState extends AgentState {
  editorContext?: { ... };
}

// After
export interface EditorAgentState extends CodingAgentState {
  editorContext?: {
    // ... 기존 필드들 ...
    enableMCPTools?: boolean;      // ⭐ NEW
    enablePlanning?: boolean;      // ⭐ NEW
    enableVerification?: boolean;  // ⭐ NEW
  };
}
```

**2. 유틸리티 클래스 통합**

CodingAgent에서 검증된 유틸리티 클래스들을 Editor Agent에 통합:

```typescript
// Static utility instances (shared across all instances)
private static toolSelector = new ToolSelector();
private static fileTracker = new FileTracker();
private static contextManager = new ContextManager(100000); // 100k tokens
private static codebaseAnalyzer = new CodebaseAnalyzer();
```

**3. executeTool 메서드 고도화**

```typescript
private async executeTool(toolCall: ToolCall, state: EditorAgentState): Promise<any> {
  // 1. Built-in Editor Tools (기존)
  if (builtinTools.includes(name)) {
    // File tracking for write/edit operations
    await FileTracker.trackBeforeModify(filePath);

    // Execute with error recovery & timeout
    const result = await ErrorRecovery.withTimeoutAndRetry(...);

    await FileTracker.trackAfterModify(filePath);
    return result;
  }

  // 2. MCP Tools (신규)
  if (state.editorContext?.enableMCPTools) {
    const allTools = await MCPServerManager.getAllTools();
    const toolInfo = allTools.find((t) => t.name === name);
    const mcpResult = await MCPServerManager.callTool(
      toolInfo.serverName,
      name,
      args
    );
    return mcpResult;
  }
}
```

**4. GraphFactory 설정 전달** (`lib/langgraph/index.ts`)

```typescript
// Inject Editor Context with advanced features
(initialState as any).editorContext = {
  ...(initialState as any).editorContext,
  // Advanced features (Cursor/Cline 수준)
  useRag: config.enableRAG || false,
  enableMCPTools: (config as any).enableMCPTools || false,
  enablePlanning: (config as any).enablePlanning || false,
  enableVerification: (config as any).enableVerification || false,
};
```

---

## 📦 변경된 파일

### 핵심 파일

1. **`extensions/editor/agents/editor-agent-advanced.ts`**
   - EditorAgentState를 CodingAgentState 기반으로 확장
   - 유틸리티 클래스 (ToolSelector, FileTracker, ContextManager 등) import
   - executeTool에 MCP Tools + Error Recovery + File Tracking 통합

2. **`extensions/editor/components/EditorChatInput.tsx`**
   - graphConfig에 enableMCPTools: true 추가
   - enableRAG: true로 변경 (기존 false)

3. **`lib/langgraph/index.ts`**
   - streamEditorAgentGraph에 enableMCPTools 설정 전달
   - workingDirectory 설정 추가

---

## 🎯 사용자에게 미치는 영향

### Before (v0.7.4 이전)

```typescript
// Editor Agent 기본 기능만 사용 가능
- 파일 읽기/쓰기
- 터미널 명령 실행
- Git 상태 확인
```

### After (v0.7.5)

```typescript
// Cursor/Cline 수준의 고급 기능
- ✅ MCP Tools (GitHub, Browser, Filesystem 등 외부 도구)
- ✅ File Tracking & Rollback (안전한 파일 수정)
- ✅ Error Recovery (자동 재시도)
- ✅ RAG (프로젝트 문서 기반 답변)
- 🔄 Planning Pipeline (향후 완전 구현)
- 🔄 Verification System (향후 완전 구현)
- 🔄 Context Management 100k (향후 완전 구현)
```

---

## 🔮 향후 계획

현재 버전에서는 기본 인프라가 구축되었으며, 다음 기능들은 향후 버전에서 완전히 구현될 예정입니다:

1. **Planning Pipeline**: 복잡한 작업을 자동으로 단계별로 분해하고 실행
2. **Verification System**: 코드 변경 후 자동으로 타입 체크, 린트, 테스트 실행
3. **Context Management**: 100k 토큰 관리로 대규모 프로젝트 효율적 처리
4. **Progress Tracking UI**: 진행 상황, 파일 변경 내역, 검증 결과 실시간 표시

---

## 📝 업그레이드 가이드

별도의 마이그레이션 작업은 필요하지 않습니다. 기존 Editor Agent 사용법은 그대로 유지되며, 새로운 기능들이 자동으로 활성화됩니다.

**MCP Tools 사용을 위한 준비:**

1. MCP Server 설정 (Settings > MCP Servers)
2. GitHub, Brave Search 등 원하는 MCP 서버 추가
3. Editor Agent에서 자동으로 사용 가능

---

## 🐛 알려진 제한사항

- MCP Tools는 첫 호출 시 서버 목록을 검색하므로 약간의 지연이 있을 수 있습니다
- Planning 및 Verification 기능은 아직 완전히 구현되지 않았습니다 (v0.7.6 예정)
- 매우 큰 파일 (10MB+) 수정 시 File Tracking이 느려질 수 있습니다

---

## 🎉 마무리

v0.7.5는 Editor Agent를 Cursor/Cline 수준으로 끌어올리기 위한 첫 번째 major step입니다. MCP Tools 통합, File Tracking, Error Recovery 등 핵심 인프라가 구축되었으며, 향후 버전에서 Planning과 Verification을 완전히 구현하여 진정한 Cursor/Cline 수준의 AI 코딩 어시스턴트가 될 것입니다.

**주요 성과:**

- ✅ MCP Protocol 완전 통합
- ✅ 안전한 파일 수정 시스템
- ✅ 자동 에러 복구
- ✅ CodingAgent 수준의 아키텍처

**다음 목표 (v0.7.6):**

- ✅ Planning Pipeline 완전 구현 (이미 완료!)
- ✅ Verification System 완전 구현 (이미 완료!)
- 🎯 Progress Tracking UI 추가
- 🎯 Context Management 100k 최적화

**Update**: v0.7.5에서 Planning과 Verification이 이미 구현되어 활성화되었습니다!

감사합니다! 🚀
