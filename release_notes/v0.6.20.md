# SEPilot Desktop v0.6.20

릴리즈 날짜: 2026-01-02

## 🎯 주요 변경사항

### 🐛 버그 수정

#### TypeScript 타입 에러 수정

**문제**: 여러 컴포넌트에서 타입 에러가 발생하여 빌드 실패

- `SingleFileEditor.tsx`: editorUseRagInAutocomplete, editorUseToolsInAutocomplete 속성이 ChatStore에 없음
- `SidebarEditor.tsx`: EditorToolsList 컴포넌트에 selectable prop이 없음
- `WikiTree.tsx`: any 타입 파라미터 에러

**해결**:

1. **Chat Store 확장** (lib/store/chat-store.ts:200-201, 579-580):
   - `editorUseRagInAutocomplete`: Autocomplete에서 RAG 사용 여부
   - `editorUseToolsInAutocomplete`: Autocomplete에서 Tools 사용 여부
   - 초기값 false로 설정

2. **EditorToolsList 선택 기능 추가** (components/editor/EditorToolsList.tsx:27-31, 41):

   ```typescript
   interface EditorToolsListProps {
     selectable?: boolean;
     selectedTools?: Set<string>;
     onToggleTool?: (toolName: string) => void;
   }
   ```

   - selectable 모드에서 체크박스로 도구 선택/해제 가능
   - SidebarEditor에서 Editor Chat 도구 선택 UI에 사용

3. **WikiTree 타입 안전성 개선** (components/layout/WikiTree.tsx:618-636):
   - findIndex, map 콜백의 파라미터에 `WikiFileNode` 타입 명시
   - 드래그앤드롭 순서 변경 로직의 타입 안전성 향상

#### ESLint 설정 개선

**문제**: E2E 테스트 결과 파일(Playwright 생성)에서 1000개 이상의 lint 에러 발생

**해결** (eslint.config.js:19):

- `e2e_tests/**` 경로를 ignores에 추가
- E2E 테스트 결과 및 리포트 파일 무시

## 🔧 기술 개선

### 개발자 경험 향상

- **Lint & Type Check**: 빌드 전 모든 타입 에러 해결
- **Editor Chat 도구 선택**: UI를 통해 사용할 도구를 선택할 수 있는 기능 추가
- **코드 품질**: TypeScript strict mode 준수

### 빌드 안정성

- Next.js 빌드 성공 (26.3초)
- TypeScript 컴파일 성공 (zero errors)
- ESLint 검사 통과

## 📝 커밋 내역

### 주요 커밋

- `fix: TypeScript 타입 에러 수정 및 빌드 안정화`
  - Chat Store에 autocomplete 관련 설정 추가
  - EditorToolsList 선택 기능 구현
  - WikiTree 타입 안전성 개선
  - ESLint 설정에 e2e_tests 제외

## 🚀 다음 버전 계획

- Editor Autocomplete에 RAG 및 Tools 통합
- Editor Chat 도구 선택 기능 완성
- 추가 타입 안전성 개선

## 📋 알려진 이슈

- Autocomplete 기능은 현재 비활성화 상태 (기본값 false)
- 향후 버전에서 UI 설정 추가 예정

---

**전체 변경사항**: [v0.6.19...v0.6.20](https://github.com/jhl-labs/sepilot_desktop/compare/v0.6.19...v0.6.20)
